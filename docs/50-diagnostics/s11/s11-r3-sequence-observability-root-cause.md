# S11-R3 Sequence Observability Root-Cause Record

## Responsibility

This diagnostic records why secure-Windows Foam findings and repository-local `sample3` full-state replay reopen detector source work. Current sequencing belongs to the work plan; accepted architecture and validation belong to the linked R3 owners.

## Reproducible sample3 review

The production detector was replayed at 2 FPS from `30.03–60.06 s` with the matching Recipe, official representative-frame static learning and one serialized temporal owner. Direct frame review used the MP4 together with `sample3.provisional-truth.json` and `sample3.oiltruth`.

Observed baseline:

- `61` scheduled rows;
- `27` numeric Oil rows and `12` accepted Foam rows;
- `41` `UNKNOWN_REVIEW`, `12` `DRAINING_VISIBLE` and `8` `FULL_WITH_FOAM` rows under explicit unknown initial state;
- `21/47` numeric Oil rows in the blind `full_like` / no-identifiable-interface interval beginning at frame `1100`; and
- no `FULL_NO_INTERFACE` publication in the reviewed interval.

At frame `1035`, the direct current-frame Oil hypothesis is near user truth (`245` versus `243`) and accepted Foam is below the physical front. That is a retained positive.

From approximately `42 s`, the dominant false Oil positions lie near source Y `213–246`. The ordinary semantic pass is usually ambiguous. Relative-contrast Spatial reconstruction then produces boundary likelihoods around `0.50–0.66`, broad relative phase strength around `0.55–0.77` and 3–5 sector paths. Source captures show these paths on the upper dark Glass cap/curved reflection while the interior is full-like.

The distinguishing conjunction is not weak edge strength. On representative false upper candidates, the phase above occupies roughly `0.01–0.10` of effective area, has a robust gray median around `15–18`, and the filled region below has a median around `69–101`. Removing that authority exposes weak lower-rim candidates around source Y `377–416`; their lower side owns only about `0.01–0.16` of effective area and lacks ordinary boundary authority. A symmetric two-phase floor was rejected during diagnosis because it removed the retained true filling observation near frame `1050`; the final D3 guard is lower-rim-only, while saturated upper conflict remains photometric. Retained true Spatial anchors have materially supported phases or a non-saturated border phase.

The Foam trace has a separate late defect. Baseline replay accepts `35` Foam rows: `12` in the real inflow/agitation interval and `23` later, including two report episodes across a drain interval annotated Foam-absent. All sample3 Foam evidence is warm/chromatic rather than white, so raising whiteness cannot solve it. The useful discriminator is row topology:

- true sample3 inflow Foam: wide-row fraction approximately `0.54–0.93`;
- genuine narrow sample2 Foam: width ratio approximately `0.47`, row compactness `1.0`;
- false sample3 late components: wide-row fraction approximately `0.13–0.48` while their broad bounding boxes have width ratio approximately `0.55–0.76`.

This supports a two-branch material-layer proof—wide row coverage or narrow compact support—without changing the score, chromatic threshold or video-specific identity.

## Existing-gate audit

The focused retained detector suite passed `461` tests on the pre-R3 head. That establishes deterministic execution of its expectations, not completeness of those expectations.

Material gaps:

- four-video fingerprints compare exact output, so they also preserve wrong output;
- the truth set has isolated point annotations but no explicit full transition contract;
- controlled no-interface fixtures do not reproduce a textured filled Glass with a dark upper cap;
- Foam controls exercise shapes and current-frame topology, but strong accepted evidence bypasses persistence, broad boxes can hide fragmented rows and there is no learned fixed-appearance opposition; and
- the sample3 temporal regression explicitly expects several outputs from the now-disproven full-state sequence.

R3 therefore adds mechanism-level two-sided fixtures and one local-video sequence contract rather than deleting the existing surfaces.

## External-reference reconciliation

The bounded review reused the sources recorded during S11-A/D1 and rechecked their original publications. They support relative/multi-line/path evidence and explicit uncertainty, but do not justify accepting every coherent path. Commercial video-analytics material likewise exposes confidence and application-specific algorithm choice without disclosing a transferable discriminator. The source decision remains repository-native: classify the proven upper-cap topology and add a bounded learned static-Foam opposition at the existing preparation/publication seam.

## Claim boundary

The private Windows video remains unavailable and cannot be a local acceptance fixture. The learned-static design is a mechanism hypothesis driven by its stable false-Foam signature; only the secure Windows replay can establish whether it closes that field defect. R3 local work must preserve this residual risk explicitly.
