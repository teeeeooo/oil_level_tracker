# S11-R17 Physical Observation Ownership Validation

## Gate

R17 is accepted locally only when the replacement mechanisms, checked-video
corpus, exact provenance, performance and full repository regression pass. It
remains `WINDOWS_PENDING` until a new private Base/Accum replay passes source
review.

## Mechanism assertions

### Oil admission and tracklets

- Candidate capacity counts distinct rows and retains bounded vertical
  diversity; duplicate representations cannot evict a lower-ranked distinct
  truth row.
- Physical confirmation receives no initial-state or entrance-band input.
- Slow anchor corridors can confirm without pretending that missing motion is
  measured zero.
- Stationary lower rows may remain physical candidates but cannot activate an
  initial-EMPTY fill owner.
- Split/merge ambiguity preserves distinct IDs and yields UNKNOWN.
- Failed tracklets expose their best measured progress, direction, motion and
  coverage beside the terminal failure reason.

### Material phase

- Initial EMPTY still requires a lower-entry upward owner.
- Initial FULL may publish a confirmed descending interface.
- A lost established fill owner may transfer only to one independently
  confirmed, compatible, clean successor within the re-entry corridor.
- A stationary, reversed, distant, ambiguous or material-opposed successor is
  rejected.
- Re-entry appends a distinct owner ID and never copies coordinates or extrema.

### Foam

- Constant glare/residue, a single motion spike and a fixed Y=80-style prefix
  do not confirm.
- Rapid rising Foam across bounded sampling gaps remains one episode.
- Narrow onset may confirm only when its later track supplies layer evolution.
- Disconnected/detached candidates do not pool merely because timestamps are
  close.
- Final Oil rejects only a repeated same-boundary track; Foam remains public
  with unknown Oil and for a valid separated layer.
- Every confirmed Foam row has exactly one selected same-frame Foam candidate.

## Repository gates

Run focused Oil/Foam owner tests, integration and observability tests, the full
repository suite, Python compilation and `git diff --check`. Run the checked
four-video replay with fingerprint regeneration treated as a review event,
never an automatic update. Any numeric delta requires direct frame review;
coverage alone is not acceptance.

## Private-Windows gate

The final field replay uses the same Base/Accum source timestamps and reviewed
source-Y anchors as R14/R16 evidence.

- Base: real Oil must be recovered around the reviewed 540/634/674 s anchors;
  reflection/bracket tracks remain rejected; public Foam is zero.
- Accum: the 480--650 s stationary false Oil remains suppressed; real
  bottom-entry Oil is admitted; truth-near rows are not lost to admission
  capacity; owner loss after 702 s can recover only through the bounded phase
  re-entry contract.
- Foam: retain the 52 reviewed real rows and reject the 50 reviewed false rows
  (Base 5 plus Accum 45), with each cohort reported separately.
- Integrity: every public Oil/Foam value has exactly one equal-Y selected nested
  same-frame candidate and equals CSV raw Y.

The Windows bundle is not a Git repository, so branch/head checks are excluded.
The bundle must still record detector/resolver versions, run identity, Recipe,
Artifact templates, trace/index and exact row counts.

R17 fails if a graph looks plausible while CSV/sequence rows are absent, if a
real value depends on interpolation, if initial EMPTY suppression regresses, or
if any public coordinate lacks same-frame provenance.
