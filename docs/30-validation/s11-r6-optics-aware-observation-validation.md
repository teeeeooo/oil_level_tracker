# S11-R6 Optics-Aware Observation Validation

**Status:** `LOCAL PASS — SECURE-WINDOWS PENDING`

## Oracle and comparison

The oracle is direct source-frame review inside each configured Glass ROI,
supported by checked-in `.oiltruth` and blind provisional annotations. R5 is the
field-failed comparison; R4 remains a fail-closed historical reference. Output
fingerprints and aggregate coverage cannot override the images.

## Gate 1 — responsibility cleanup

Tests and source inspection must prove:

1. unconfirmed Foam is absent from Oil pipeline inputs;
2. final Oil selection consumes only explicitly eligible same-frame candidates;
3. state emission does not read a projected `fill_state`;
4. the initial state contributes only an initial prior;
5. prior-only state samples are invalid in public TrackingSample output;
6. rejected/raw Foam cannot be confirmed by the later episode resolver; and
7. superseded R5 runtime modules/call paths are deleted after migration.

## Gate 2 — optics and Foam controls

Controlled fixtures must cover saturated glare, unsaturated vertical caustic
ridges, static textured reflection, one droplet, coherent dynamic Foam and real
Oil with partial glare. The expected result is high-precision Foam publication:
all Foam-absent controls publish no Foam, while retained real Foam episodes remain.

Temporal Foam evidence must be exposure-compensated and bounded. A global
brightness step or subpixel mask jitter alone cannot confirm an episode.

Pixel-identical latent-cause collision pairs must remain non-numeric even after
repetition. A dynamic material twin may regain anchor authority only from
registered internal change, and removing that allowance must not be accepted if
it creates a visually false long graph gap.

## Gate 3 — Oil/state mechanics

Unit tests must prove:

- a fixed optics-supported row loses to a weaker moving material path;
- a strong stationary material interface remains eligible;
- a missed edge entrance does not lock an initial EMPTY/FULL prior forever;
- no-interface state requires raw typed likelihood on each valid sample;
- hard-unavailable frames remain UNKNOWN; and
- no output coordinate exists without same-frame candidate provenance.

## Gate 4 — checked-in videos and direct images

Replay all four videos with their matching Recipe, truth and provisional files.
Review representative source overlays for every trajectory run, Foam episode,
state transition and extremum. At minimum record:

- visually supportable Oil coverage and truth-anchor MAE;
- gross wrong-interface duration/longest run;
- Foam false frames/episodes on annotated absent intervals;
- first-acquisition latency after visible entry;
- image-supported versus prior-only state duration; and
- event/capture agreement after detector acceptance.

Sample3 `30–60 s` must tell the EMPTY/inflow/Foam/full story without turning a
fixed row into the track. sample2's stationary Oil and genuine sample3/sample4
Foam are mandatory non-regressions. Visually unclear/Base-overlay spans may stay
UNKNOWN.

## Gate 5 — repository regression

Run focused optics/Foam/Oil/state tests, all controlled benchmarks, production
analysis/report integration and the complete test suite. Runtime remains within
the existing bounded CPU contract. Changed fingerprints require image-based
reconciliation.

## Secure-Windows holdout

The exact committed R6 head must be replayed on the private Base/Accum video.

- Base: zero published Foam when direct review shows none; retain initial FULL as
  context, then acquire and follow the descending/recovering real interface.
- Accum: retain EMPTY only while raw image evidence supports it; acquire the rise
  before mid-Glass lock-in, retain only the bounded turbulent Foam episode and
  follow high/fall.
- both: no fixed glare/caustic Oil run, no prior-only valid coverage, and event
  timestamps derived from accepted observations.

Any Base false Foam episode or Accum lock-in through the visible transition is a
field FAIL independent of overall coverage. The private result must be recorded
before S11 can close.

## Local disposition

Gates 1–5 pass on the checked-in corpus at `f2369ab`. The exact replay and
direct-image reconciliation are recorded in the
[R6 local evidence](../60-evidence/s11/s11-r6-optics-aware-observation.md).
This status does not satisfy the secure-Windows holdout.
