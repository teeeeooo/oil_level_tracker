# S11-R4 Field Residual Authority Evidence

**Status:** `LOCAL_ACCEPTED — SECURE WINDOWS REQUIRED`

**Task baseline:** `ac3b02b3b50e70f8f50685bc518a8ccf96b89ed0`

**Detector implementation commit:** `745c6c923bb7e640ae0895f88c6bdf300198914a`

**Report continuity / validated source-test head:** `cc3d358ee735dc433692dbbe3722057bd0c2ae9a`

## Outcome

R4 locally accepts a bounded repair for the residual authority seams found by the first secure-Windows R3 replay. It does not claim that the unavailable Base/Accum field defect is closed.

The accepted source behavior is:

- learned static Foam keeps the exact `0.80` rejection and adds a conjunctive registered path using exact, distance-tolerant and reciprocal coverage;
- a new strong Foam component needs two front-compatible samples before public Foam, fill-state and tracker publication;
- the first coherent/non-static strong sample still applies the existing D5 Oil front/mask safety constraint without becoming public Foam;
- hard unavailable/failure/glare evidence and Foam-only prefixes remain retrospective barriers, while independently valid Foam-plus-canonical-Oil samples retain numeric direction/topology authority;
- `OIL_DROP_START` is presented as the first observed sustained decrease and is never backdated; and
- report grouping accepts already temporal-confirmed singleton publication and uses stored pending flags only to connect/defer disappearance inside the existing bounded dropout rule. Pending evidence never starts an episode or supplies a Foam coordinate.

No Oil/Foam evidence threshold, Canny/Hough parameter, zero-line sign, public Recipe/result schema, numeric interpolation, sample identity branch, raster history or second Oil temporal owner changed.

## Design basis

The causal input is the [secure-Windows residual diagnostic](../../50-diagnostics/s11/s11-r4-windows-residual-authority-diagnostic.md): R3 reduced Base `FULL_WITH_FOAM` from `95.7%` to `32.1%`, but the residual exact static overlap population averaged `0.793`; strong Foam still published immediately and retrospective interpretation checked Foam before same-sample canonical Oil.

The design reuses the accepted S11 architecture and the prior external review in the [implementation reference log](../../70-reference/implementation-reference-log.md). Transparent-vessel research supports relative/multi-line/path evidence and treats optics/illumination as material competing causes. The commercial reference supports explicit uncertainty but provides no transferable Foam discriminator. That record supports registered fixed-appearance opposition and bounded temporal confirmation, not global threshold relaxation or a new model.

The repository-local Oil-static probe remained counterevidence to a hard static-Oil veto: retained sample2 Oil reached approximately `0.260` static overlap and controlled structure reached approximately `0.18`. R4 therefore leaves S5-B Oil thresholds and static scoring unchanged.

## Mechanism validation

Controlled tests prove:

- exact rejection at `0.80` and all three registered conjunction boundaries (`0.70 / 0.90 / 0.70`);
- bounded `1–3 px` registration, shape mismatch failure, materially larger-support preservation and transient preparation-frame exclusion;
- strong pending → accepted → continuously accepted lifecycle, incompatible-jump restart, reset isolation and bounded scalar-only state;
- moderate/strong transition counts, row-incoherent/static rejection and diagnostic retention;
- pending strong D5 safety without public Foam, and no such authority for static/incoherent evidence;
- Foam-only retrospective barrier, valid Foam-plus-Oil direction evidence, projection exclusion and hard-glare precedence; and
- observation-qualified Oil-drop wording, confirmed singleton Foam reporting, pending-only continuity and no report backdating.

The legacy controlled benchmark's frame-level Foam recall is now `2/6` by construction because it mixes three independent singleton truths with one three-frame sequence. This is not accepted as a detector-recall regression: all singleton strong observations are intentionally pending, while the persistent sequence is exactly `pending → accepted → accepted` with precision `1.0`. Current-frame raw evidence and sequence publication are asserted separately.

## Exact four-video replay

The accepted replay uses matching MP4/Recipe/truth inputs, official start/middle/end static learning, production `OpenCvPhaseDetector`, one serialized Oil owner and `2 Hz` qualification windows. It was executed with accepted-count verification enabled into ignored forensic output under `sample/output/s11-r4-accepted-local/`.

| Video | Numeric Oil | Tracking fingerprint | Accepted / pending Foam rows | Report Foam episodes |
|---|---:|---|---:|---:|
| `base_sample_1` | `2/30` | `c50e1e8bb7ff8bda873c1fbd4637586de4798fb950ea9fb17213ec8d3eb9716c` | `0 / 0` | `0` |
| `sample2` | `2/5` | `392a9a05f03e2831dcfcedc52cb0fa55743022d3d150fab15e6a23a874977eda` | `1 / 2` | `1` |
| `sample3` | `21/151` | `f3caf5e79e7663ca9a3d28b2a173c6237d886ae4e5c19482ad31f07aea481fd9` | `5 / 7` | `1` |
| `sample4` | `64/113` | `23b0568b0184f6a7972947d48d52e9122214d419ba67f4c6c5c0cd351116d3fb` | `17 / 10` | `2` |
| **Total** | **`89/299`** | — | **`23 / 19`** | **`4`** |

Numeric Oil is unchanged from R3. The user-confirmed truth surface remains exactly `8/13`, with `5.4375 px` MAE, `6.5 px` median error and `11 px` maximum error. Existing glare, collision, structure, no-interface and sample3 cap negatives remain fail-closed.

The natural four-video static-Foam maps produced no non-zero overlap or registered-dominant row; this mechanism remains controlled-fixture evidence locally and must be measured on the private Base Glass. Foam publication changed from R3's `0 / 3 / 12 / 27` accepted rows to `0 / 1 / 5 / 17`. The bounded confirmation reduces isolated publication while preserving all three truth-backed report surfaces:

- sample2 has one confirmed episode at `1.50 s`; the source capture visibly contains a dense bubble layer;
- sample3 consolidates the true inflow/agitation observations into one `30.53–39.04 s` report episode and retains no late Foam; and
- sample4 retains two material report episodes beginning at `40.00 s` and `45.00 s`.

Report output contains `22` bounded landmarks and `21` Glass-focused source captures. Pending flags consolidate the sample3 episode but do not fill the orange Foam graph, create coordinates or change tracking/event history.

## Direct visual reconciliation

The generated detail graphs and decoded source captures were inspected against the videos:

- sample2 `1.50 s` shows the dense turbulent Foam layer at the stored blue guide;
- sample3 `30.53 s`, `34.53 s` and `37.04 s` show the same physical inflow/agitation progression, now represented by one episode rather than repeated false disappearance;
- sample3 `99.03 s` places the stored minimum Oil guide on the visible later drain boundary;
- sample4 `37.50 s` / `39.50 s` highest/lowest guides align with the visible interface; and
- Base and the blind Foam-absent late sample3 interval produce no report Foam episode.

The long sample3 missing-Oil span remains a dashed display-only bridge. It is not treated as detector accuracy, an interpolated trajectory or evidence for an earlier physical Oil-drop timestamp.

## Validation record

Final source-tree validation on the closeout worktree:

```text
PYTHONPATH=src .venv/bin/python -m pytest -q
1485 passed in 110.31s

PYTHONPATH=src .venv/bin/python -m tests.diagnostics.s11_report_observability_replay
# accepted-count/fingerprint-equivalent replay executed through run_replay(...)
# 299 rows, 89 numeric Oil, 22 report landmarks, 21 captures

git diff --check
.venv/bin/python -m compileall -q src tests/diagnostics
```

The refreshed offline trajectory manifest is bound to implementation commit `745c6c923bb7e640ae0895f88c6bdf300198914a`, preserves `89` observed / `210` unavailable rows and the `insufficient_trajectory_evidence` conclusion, and has fingerprint `61a88c4d618219287443971ab606ea4c58494b09359ece93f78ea45c84a30237`.

## Remaining secure-Windows gate

The same private Base/Accum bundle must now record exact/tolerant/reciprocal overlap distributions, registered-static and onset-pending counts, accepted Foam episode timing, fill-state/numeric Oil/retrospective distributions and source-coordinate overlays. Acceptance requires materially less residual Base false Foam while preserving the bounded real Accum Foam interval.

If registered overlap does not explain the remaining Base publication, stop at the earliest current component metrics. If Oil coordinates remain displaced, inspect the accepted candidate and configured zero/source overlays before proposing any sign or offset change. This local evidence does not authorize global Foam/Oil tuning.
