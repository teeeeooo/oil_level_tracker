# Current Work Plan

**Current milestone:** `S11 — Real-Field Detector Effectiveness Recovery`
**Milestone status:** `ACTIVE`
**Current engineering gate:** `bounded S5-B current-frame semantic repair — accepted-Foam hard-invalid hypotheses must not retain D3 comparative anchor authority`
**Provisional lane:** `Lane B — bounded current-frame semantic authority repair`

## Current decision

The completed post-D5 read-only effectiveness reconciliation re-measured the accepted D1–D5 baseline rather than carrying forward pre-D3/D4/D5 attribution. It identified one bounded **current-frame semantic authority** seam as the next source responsibility:

> An Oil hypothesis already made hard-invalid by authoritative accepted-Foam topology must not retain D3 comparative neighborhood/anchor authority over hard-safe alternatives.

This is narrower than a detector retune and narrower than temporal redesign. The next source slice must repair only that authority composition boundary and preserve the accepted safety/ownership stack.

## Accepted baseline to preserve

S11 remains active on an accepted baseline that already includes:

- typed S5-B observability and canonical fail-closed publication;
- P2/FULL-EMPTY preservation and bounded Spatial current-frame corroboration;
- D1 Foam/Oil context safety;
- D2 Class-A positive Spatial recovery and Class-B additive observation/proposal representation;
- D3 comparative current-frame semantic authority slimming;
- D4 Foam support representation and structural-component classification;
- D5 Foam-constrained D2/D3 authority continuity;
- one serialized production temporal owner.

Durable responsibility details live in [`../20-architecture/s11-detector-responsibility-architecture.md`](../20-architecture/s11-detector-responsibility-architecture.md). Historical implementation/audit records live in [`../60-evidence/s11/`](../60-evidence/s11/).

## Current source-slice boundary

The repair must keep authoritative accepted-Foam topology hard: an Oil hypothesis invalidated by that topology cannot publish. The remaining defect is indirect authority: the same hard-invalid hypothesis must also be removed from any D3 anchor/neighborhood role that can suppress or delimit comparison of alternatives that are still hard-safe.

The repair must not create a new semantic stack, relax no-interface/glare/structure safety, bypass Foam-component exclusion, add truth/frame/video-specific routing, or add post-owner numeric reconstruction. D3 local near-tie behavior remains fail-closed among candidates that are still hard-safe.

The lane remains **provisional Lane B** until the exact source owner/diff is inspected. A materially broader responsibility discovered during implementation must be reclassified rather than silently absorbed.

## Temporal and closure boundary

Production temporal redesign remains **unapproved**. The offline temporal trajectory probe found insufficient evidence for interpolation/trajectory publication and remains diagnostic provenance under [`../50-diagnostics/s11/`](../50-diagnostics/s11/). Residual misses after the current-frame repair do not by themselves authorize temporal work.

S11 stable-baseline closure is **not yet justified**. Repeated private Windows field-video replay is no longer a normal iterative dependency. One final Windows field-workflow check is retained only after a stable S11 baseline exists; see [`retained-commitments.md`](retained-commitments.md).

## Validation route

The current source slice is governed by [`../30-validation/s11-real-field-detector-effectiveness.md`](../30-validation/s11-real-field-detector-effectiveness.md). Use bounded exact current-frame reproductions plus materially adjacent accepted regressions; do not substitute historical aggregate counts for current authority.

Diagnostic and evidence routing:

- S11 diagnostics/manifests: [`../50-diagnostics/s11/`](../50-diagnostics/s11/)
- completed S11 implementation/audit evidence: [`../60-evidence/s11/`](../60-evidence/s11/)
- retained/deferred work: [`retained-commitments.md`](retained-commitments.md)

## Next handoff

After this Lane A documentation restructuring is independently verified in a fresh read-only Orchestrator session, carry forward the engineering gate unchanged:

**bounded S5-B current-frame semantic repair — accepted-Foam hard-invalid hypotheses must not retain D3 comparative anchor authority; provisional Lane B.**
